# Normalize Package

Canonical post-extraction normalization lives here.

Current Phase 1 responsibilities:

- parse resolved extraction snapshots into normalized protocol artifacts
- normalize measurement strings for concentrations, temperatures, durations, and rates
- preserve additive traceability back to extraction evidence

This package does not replace extraction, benchmarking, or override logic. It derives a cleaner comparison layer on top of the resolved extraction state.
